things to add to pattern instances:
  @parent (set by route)
  path readers (@...)
  uri (right now, from parse -> @uri)
  redirect (uses uri)
  attributes ->
    render (xml/yaml attributes)
    attribute accessors

