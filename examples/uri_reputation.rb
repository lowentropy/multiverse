describe "URI reputation" do
  it "should record a reputation for a uri" do
    pending "functionality"
    "http://some_host/grid/(uid-of-object)".to_entity.update :reputation => N
    rep(uid-of-object).update N
    some_object.reputation << a_new_rating
  end
end